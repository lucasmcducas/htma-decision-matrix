# HTMA Kids Matrix Viewer — local React/TypeScript dev artifact

Run: `npm install && npm run dev` (port 5173).

Data source: `public/data.json` is generated by
`/home/luke/.openclaw/workspace/htma-decision-matrix/tools/build_viewer_data.py`
from the v2 matrix repo. After editing matrix YAMLs, re-run that script
and the viewer reflects the changes.

Backed up from `/tmp/htma-matrix-viewer/` (Sep 18, 2026) at the
"good place" baseline — see commit f2ca667 in the tmp repo for the
exact snapshot before 3-Lows hybrid work began.
